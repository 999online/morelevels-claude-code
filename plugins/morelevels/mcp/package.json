{
  "name": "morelevels-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency MCP server wrapping the morelevels API (submit level + read progression + save token).",
  "bin": {
    "morelevels-mcp": "./server.js"
  },
  "scripts": {
    "selftest": "node server.js --selftest"
  },
  "engines": {
    "node": ">=18"
  }
}
