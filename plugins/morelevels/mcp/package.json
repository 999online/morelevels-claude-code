{
  "name": "morelevels-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server wrapping the morelevels API (submit level + read progression).",
  "bin": {
    "morelevels-mcp": "./server.js"
  },
  "scripts": {
    "selftest": "node server.js --selftest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
